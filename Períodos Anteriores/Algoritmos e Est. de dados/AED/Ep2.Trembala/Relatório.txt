 O problema foi resolvido através do calculo da distancia entre as cidades e a comparação da distancia entre essas cidades. 
Desta forma foi possível saber quais as cidades e a menor distancia entre elas. 

O código está divididos em "quatro" partes:

- Criar a função "ordenacaoMSort";
- Criar a função "ordenacaoBBSort";
- Criar função "distancia";
- Leitura e traramento do arquivo de entrada


    *A função "ordenacaoMsort" pega a lista que lista de distancias de cidades e ordena em grau Teta(n log 2n) no pior casou e devolve a lista ordenada para "pista".

    *A função "OredenacaoBBSort" por sua vez é a função que recebe a lista L( abcissa) e lista E(ordenada) do arquivo e ordena apenas lista L, pois a lista E vai fica arraigado na lista L. Assim 
     L e E poderão trablhada melhor!
    
    *A função "distancia" pega duas cidades e calcula a distancia entre a podemos depois comparar todas as cidades e saber quais as cidades que tem a menor distancia entre elas.
    
    *Leitura  tratamento do arquivo de entrada
    	# "N" é o comprimento de cada lista de  cada país que está em trembala.dat
    	# "abcissa" é lista que vai ser ordenada na função "ordenacaoBBSort", 
    	# "ordenada" é lista que vai ser ordenada na função "ordenacaoBBSort"

    	# "compara_d" Numero de interos que vai ser maior do "d"   
   	# "receb_distancia" é a variável que é utilizada para receber a distancia de cada cidade.
    	# " menor_dis" lista de distancia entre as cidades.
      
	
   	 '''ordenacaoBBsort e ordenacaoMSort vem dentro do "while true" que é a parte de tratamento dos dados e onde todo o programa é feito pois as outras funções chamadas dentro do
    	    While que tem complexidade o(n^2).
    	 '''


    Em #abrir o arquivo e ler, retira o "\n" e fecha o arquivo, é onde o programa faz a leitura do arquivo como string.

    Em #transforma uma lista de strings em uma lista de inteiros, é onde pega o que foi lido em #abri o arquivo e ler, retira o "\n" e fecha o arquivo, e transforma em uma lista de inteiros, para que essa lista seja trabalhada dentro do programa e tem grau O(n).



--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  
